/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: "/projetoportfolio",
  output: "export",  // <=== enables static exports
  reactStrictMode: true,
};

module.exports = nextConfig;
